import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function main() { const { error } = await supabase.from('transaksi_pendapatan').insert([{ tanggal: '2026-05-21', unit: 'TK', sumber_dana: 'Dana Pesantren/Yayasan', nominal: 1000, jenis_penerimaan: 'Transfer', nama_bank: '-', keterangan: 'Test' }]); console.log('ERROR:', error); } main();
