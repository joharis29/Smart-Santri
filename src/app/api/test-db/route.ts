import { NextResponse } from 'next/server'; import { createAdminClient } from '@/utils/supabase/server'; export async function GET() { try { const supabase = createAdminClient(); const { error } = await supabase.from('transaksi_pengeluaran').insert([{ tanggal: '2026-05-21', unit: 'Pusat (Yayasan)', sumber_dana: 'Dana SPP', nominal: 999999999999, metode_pencairan: 'Transfer', nama_bank: '-', keterangan: 'Test' }]); return NextResponse.json({ error: error || null }); } catch (err: any) { return NextResponse.json({ error: err.message }, { status: 500 }); } }
